# Places API vs OpenStreetMap — trade-off

General comparison table moved to the main vault: `wiki/synthesis/places-api-vs-openstreetmap.md`.

**HungryNow decision:** switched to Google Places API for data quality, accepting the billing/card requirement, mitigated with a hard quota cap + app-level counter (see [[google-places-api]]).
