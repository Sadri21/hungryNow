# Moya

Networking abstraction layer over Alamofire, using the `TargetType` protocol pattern. General rationale for choosing Moya lives in the main vault: `wiki/entities/tools/moya.md`.

**HungryNow-specific:** see [[architecture]] for where it sits in this app's folder structure — always behind `RecommendationServiceProtocol`, never called directly from a ViewModel.
